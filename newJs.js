const user = {
  name: "Obi",
  age: 29,
  nationality: "Ghana",
  bundle: "free"
}

alert(`Current User is ${user.age}-year-old ${user.name} from ${user.nationality} using the ${user.bundle} bundle.`)