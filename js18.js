//create a recursive function that returns the fibionacci sequece

function createFibonacciSequence(layers) {
    let entryNumber = 0;
    let newNumber = 1;
    console.log(newNumber)
    function returnFibonacciSequence() {}
}
